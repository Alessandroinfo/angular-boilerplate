import { BoxLineDirective } from './box-line.directive';

describe('BoxLineDirective', () => {
  it('should create an instance', () => {
    const directive = new BoxLineDirective();
    expect(directive).toBeTruthy();
  });
});
