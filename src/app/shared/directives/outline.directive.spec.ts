import { OutlineDirective } from './outline.directive';

describe('OutlineDirective', () => {
  it('should create an instance', () => {
    const directive = new OutlineDirective();
    expect(directive).toBeTruthy();
  });
});
